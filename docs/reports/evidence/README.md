# Alpha verification evidence

Committed JSONL traces from unattended installed-package runs live here. Each line is one
timestamped observation. Screenshots stay outside the public repository under
`~/sb88/evidence` because the desktop can contain personal windows.
